# FM 그래픽 새로고침 1.0.0 / FM Graphics Reload 1.0.0

**후원하기:** 프로그램 오른쪽 맨 아래 **후원하기**에서 Ko-fi QR을 스캔하거나 **후원 페이지 열기**를 누르세요. 모든 기능은 무료이고 후원은 선택입니다. 후원 창을 자동으로 띄우거나 후원 여부에 따라 기능을 제한하지 않습니다. 후원은 사용자가 외부 Ko-fi 페이지에서 직접 진행합니다. 기본 언어는 영어이며 오른쪽 위에서 한국어로 바꿀 수 있습니다. 기존 언어 설정은 유지합니다. 게임 연결 모듈은 0.17~0.22와 같습니다.

1.0.0에서는 선명한 고배율 표시를 유지하면서 글자와 창·표·토글·간격의 배율을 통일했습니다. 기본 650×480, 최소 620×450은 논리 픽셀 기준입니다. 200% 화면에서는 기본 1300×960 실제 픽셀로 표시됩니다. 폴더 입력란은 두 줄이 정렬되고 한글·영문 열 제목에 맞춰 표 너비가 계산됩니다. 아이콘은 원본 그림을 그대로 유지하고 바깥 투명 여백만 줄였습니다. 0.17과 동일한 연결 모듈이므로 0.17 연결 상태에서 추가 재시작이 필요하지 않습니다.

단일 EXE 파일인 `FM Graphics Reload 1.0.1.exe`를 실행하세요. 배포본에는 스킨·페이스·로고·사용자 백업이 들어 있지 않습니다. 1.0.0은 메모리 관리와 검색을 변경한 연결 모듈을 사용합니다. 0.16 이하 모듈이 연결된 FM은 한 번 종료하고 다시 실행해야 합니다. FM이 꺼진 상태에서 업데이트했다면 다음 실행부터 새 모듈을 사용합니다.

스킨 조합이 달라져도 검증된 동일 번들 및 복원 자료를 공유합니다. 서로 참조하는 파일은 묶음 전체가 일치할 때만 재사용하며, 변경된 묶음만 새로 불러옵니다. 이전 전체 자원 조회 자료는 새 자료가 준비된 뒤 해제합니다. 실제 스킨 자원과 게임의 참조는 유지하므로 여러 다른 스킨을 계속 불러오면 보관 한도에 도달할 수 있습니다. 40,000개 연결 제한은 동적 할당과 이진 검색으로 개선했으며 복원 순서는 유지합니다.

작업 표시줄 아이콘은 현재 화면 배율에 맞춰 선택합니다. 18개 ICO 해상도와 256·512·1024픽셀 PNG를 포함합니다. 200% 배율에서는 작업 표시줄용으로 64픽셀 아이콘을 사용합니다. Windows에 이전 EXE가 고정돼 있으면 이전 고정을 해제하고 새 EXE를 고정하세요.

1.0.0에서는 중복 전체 자원 조회 두 번을 제거했습니다. 스킨 준비 파일을 한 번 만든 뒤에는 게임을 다시 실행해도 파일과 스크립트 정보가 같으면 재사용합니다. 새 게임의 메모리 자원 로딩·호환성 검사·화면 연결은 계속 수행합니다. 처음 준비하는 스킨, 파일 변경 또는 캐시 초기화 후에는 준비 시간이 다시 필요합니다. 캐시 파일이 손상되면 새로운 사본을 만들며 기존 설치 파일이나 백업을 변경하지 않습니다.

스킨의 첫 로딩에서 스크립트 연결이 누락되면 원본의 공개 자원 목록과 대조하고 새 사본으로 한 번 재준비합니다. 양쪽 사본에서 같은 자원이 누락돼도 차단하며, 이름뿐 아니라 클래스와 개수도 확인합니다. 첫 적용은 재준비 때문에 더 오래 걸릴 수 있습니다. 실패 사본은 화면 적용에 사용하지 않으며 게임 종료 때까지 메모리에 보관합니다. 해상도 설정의 실행 중 초기화도 추가했습니다.

1.0.0는 메뉴 레이어와 열린 패널을 새 스킨의 템플릿으로 다시 만듭니다. FM 전용 템플릿 연결을 사용하고 열린 화면의 데이터 연결을 새 패널에 이어 주므로, 26to24의 왼쪽 메뉴처럼 배치가 바뀌는 스킨도 실행 중 반영할 수 있습니다. 하위 템플릿을 만들지 못하면 화면을 교체하기 전에 중단합니다.

## 1.0.0의 실행 중 교체 제한

누락·신규 번들을 실행 중 로드하지 않도록 ON 전환, 적용 버튼, 작업 시작, 실제 번들 로드 직전과 화면 참조 교체 직전에 각각 검사합니다. 이전 설정에서 이미 ON인 항목도 화면 적용 검사 대상입니다. 제한된 항목을 OFF로 바꾸거나 삭제하는 동작은 허용합니다.

- 실행 중인 fm.exe에서 확인한 정확한 StandaloneWindows64 폴더만 검사합니다. 다른 설치 경로나 중첩 폴더로 대체하지 않습니다.
- 파일 목록은 프로그램이 해당 FM 실행을 처음 관찰할 때 기록합니다. 생성·수정 시각이 게임 시작 이후인 파일, 처음 기록에 없는 파일, 기록 이후 바뀌거나 사라진 파일은 실행 중 적용을 차단합니다. 이는 로드된 자원 전체의 호환성을 증명하는 검사는 아닙니다.
- 게임 프로세스에 접근할 권한이 없어도 프로세스 이름으로 실행 여부를 확인합니다. 설치 위치나 파일을 검증할 수 없으면 FM이 꺼진 것으로 간주하지 않고 적용을 차단합니다.
- UI 이외 번들을 일괄 차단하지 않습니다. 파일 이름의 종류와 관계없이 기존 파일 확인 절차를 사용합니다. 스킨 이름에 마우스를 올리면 제한 사유를 표시합니다.
- 파일 존재 확인은 내부 자원 호환성이나 모든 크래시 방지를 보장하지 않습니다. 내부 자원 대응 검사도 유지합니다.
- 허용된 번들은 실제 화면 갱신 동안 읽기 핸들을 유지하여 외부 쓰기·삭제를 제한합니다. 보호를 확보할 수 없으면 로드 전에 중단합니다.
- 추가 파일을 설치하는 작업은 계속 FM 종료 상태에서만 가능합니다. 게임 실행 중 등록 자체는 OFF 상태로 가능하지만, 제한된 스킨을 ON으로 전환하거나 화면에 적용할 수는 없습니다.
- 이전 실행 기록도 UI 여부로 차단하지 않습니다. 누락·신규·변경 파일이 있으면 해당 사유로 차단합니다.

창의 기본 크기는 650×480, 최소 크기는 620×450입니다. 표 행 높이와 여백·버튼·폴더 행을 줄이고 밝은 회색과 흰색, 파란색 강조로 정리했습니다. 긴 오류 메시지 때문에 창 레이아웃이 늘어나지 않도록 상태 영역 높이를 제한합니다. 전체 제한 사유는 스킨 이름의 도움말과 오류 기록에서 확인할 수 있습니다.

창 제목은 언어에 따라 다음과 같습니다.

- 한국어: **FM 그래픽 새로고침 V1.0.0 (제작 : JunHo)**
- English: **FM Graphics Reload V1.0.0 (by.JunHo)**

**1.0.0 temporary live restriction:** missing, newly introduced or changed-since-start bundles are blocked before enabling and before native loading/apply. There is no UI-only filename whitelist. Verified existing non-UI bundles follow the same checks. Existing ON selections are also checked, while turning them OFF remains possible. Exact process installation discovery and read handles prevent fallback-path acceptance and changes to checked files during reload. This restriction does not establish the cause of every crash or certify every bundle as safe.


## 스킨 ON/OFF와 적용 버튼

ON/OFF, 우선순위, 삭제는 게임 연결 확인이나 게임 대기 상태를 요구하지 않습니다. 게임이 꺼져 있어도 사용할 수 있습니다.

- **FM이 꺼져 있을 때:** ON/OFF·우선순위 변경을 실제 설치 파일에 바로 반영합니다. ON은 기존 파일을 먼저 백업하고 교체하며, OFF는 다음 우선순위의 ON 스킨이나 백업 파일을 적용합니다. 처음부터 없던 파일을 프로그램이 추가했다면 마지막 제공 스킨을 OFF 할 때 그 파일을 삭제합니다.
- **FM이 켜져 있을 때:** ON/OFF·우선순위 변경은 선택 목록에 저장합니다. 게임이 사용 중인 설치 파일을 직접 교체하지 않습니다. 현재 게임 화면은 **스킨 적용** 또는 **전체 적용**으로 갱신합니다.
- **FM을 종료하면:** 이 프로그램이 열려 있으면 대기 중인 선택을 설치 파일에 자동 저장합니다. **설치 파일 반영 완료** 안내를 확인한 뒤 FM을 다시 켜세요.
- 이번 실행에서 새로 변경한 선택이 설치 파일 반영 대기 중일 때 닫으려 하면 안내합니다. 시작 후 변경 없이 닫으면 확인창을 반복하지 않습니다. 닫기로 선택해도 선택 목록은 남습니다. FM이 꺼진 상태에서 프로그램을 다시 열면 저장을 재시도합니다. 프로그램을 닫은 동안에는 종료 감시나 파일 저장을 수행하지 않습니다.
- 자동 저장에 실패하면 반복해서 덮어쓰지 않습니다. 원인을 해결한 뒤 삭제 옆 **▾ → 대기 중인 설치 파일 저장 재시도**를 사용하세요.

화면 적용 버튼은 FM이 실행 중이고 연결된 게임이 대기 상태일 때 사용할 수 있습니다. 실제 작업 직전에도 기존 진행 잠금을 확보합니다. 화면 갱신과 설치 파일 저장은 별개의 작업입니다.

0.10에서 발생한 실행 중 `WinError 5`는 게임이 사용 중인 번들을 새 파일로 바꾸는 단계에서 발생했습니다. 1.0.0은 실행 중 화면 갱신에 등록 스킨·백업의 복사본을 사용하고, 실제 설치 파일 교체는 FM 종료 후로 분리합니다.

## 설치 폴더에 없는 파일

스킨을 ON으로 바꾸기 전에 선택한 스킨의 `.bundle` 파일 이름과 설치 폴더의 파일 목록을 비교합니다.

- **FM 실행 중 누락 파일 발견:** 파일 이름과 함께 “FM을 종료한 뒤 스킨을 ON으로 켜면 추가할 수 있습니다”라고 안내합니다. 그 ON 변경은 저장하지 않습니다.
- **FM 종료 상태:** 없는 번들도 설치 폴더에 추가합니다. 기존 파일의 교체와 새 파일의 추가를 작업 기록에 구분하여 저장합니다.
- **OFF·삭제:** 프로그램이 새로 추가한 파일은 다른 ON 스킨이 사용하지 않을 때 삭제합니다. 교체했던 파일은 백업으로 복원합니다.
- 외부에서 수정된 파일은 자동으로 덮어쓰거나 삭제하지 않습니다. 수정한 파일을 새 기준으로 삼으려면 아래의 스킨 기준 초기화를 사용하세요.

이 검사는 파일 존재 여부에 대한 검사입니다. 파일을 추가한다고 FM의 자원 카탈로그가 자동으로 수정되는 것은 아닙니다. 별도 카탈로그·설치 절차가 필요한 스킨은 제작자의 설치 조건도 충족해야 합니다. 프로그램의 가져오기 대상은 `.bundle` 파일입니다.

## 삭제 오른쪽 ▾ 메뉴

### 스킨 기준 초기화

설치 파일을 직접 수정한 사용자를 위한 기능입니다. **FM을 종료한 상태**에서 사용할 수 있습니다.

- 현재 설치 폴더의 파일을 그대로 유지하고 새 기본 기준으로 사용합니다.
- 모든 등록 스킨의 선택을 OFF로 바꾸고 대기 중인 파일 저장을 취소합니다.
- 이전 적용 상태는 `installations/<설치위치 식별값>/history`에 보관하며, 복구용 백업과 등록 스킨 목록은 삭제하지 않습니다.
- 이후 처음 ON으로 교체할 때 현재 설치 파일을 새 복원 기준으로 백업합니다.
- FM의 공장 기본 스킨으로 되돌리는 기능은 아닙니다. 직접 수정한 설치 파일을 보존하려는 기능입니다.
- 중간에 끊긴 파일 교체 작업이 있으면 먼저 복구해야 하므로 초기화를 중단하고 안내합니다.

### 캐시 초기화

**FM을 종료한 상태**에서 프로그램의 임시 적용 자료를 삭제합니다.

- 삭제 대상: `cache`, `logs/full_skin`, `logs/skin_sessions`, `logs/live_inventory`.
- 유지 대상: 실제 FM 설치 파일, 등록한 스킨, 설정, 설치 파일 복구용 백업, 일반 오류 기록.
- FM 자체의 모든 캐시나 문서 폴더의 페이스·로고 원본을 삭제하는 기능은 아닙니다.
- 실행 중 모듈이 참조하는 자료를 지우지 않도록 FM 종료를 확인합니다.

저장 대기 중인 변경이 있으면 같은 메뉴에 **대기 중인 설치 파일 저장 재시도**도 표시합니다.

## 스킨 추가·이름·우선순위

**+**에서 폴더 또는 ZIP을 추가하거나 탐색기에서 프로그램으로 드롭하세요. 하위 폴더 중 `.bundle` 파일이 있는 각 폴더를 별도 스킨으로 등록합니다. 필요한 파일을 `%LOCALAPPDATA%/FM Graphics Reload/skins/folders`에 복사하고 OFF 상태로 추가합니다. 이후 원래 폴더나 ZIP을 옮겨도 등록한 복사본을 사용할 수 있습니다.

- 목록 위쪽이 높은 우선순위이며, 겹치는 파일은 가장 높은 ON 스킨이 제공합니다.
- 스킨이 8개 파일만 제공하면 그 파일과 이전 구성에서 복원해야 할 파일만 처리합니다. 전체 설치 폴더를 매번 복사하지 않습니다.
- **선택파일**은 최종 선택된 파일 수, **밀린파일**은 우선순위에서 밀린 파일 수입니다.
- 이름은 마지막 폴더를 표시하고 상위 경로는 `..\..\폴더이름`처럼 줄입니다. 마우스를 올리면 전체 이름을 표시합니다.
- **Ctrl+클릭 / Shift+클릭 / Ctrl+A**로 여러 행을 선택합니다. **↑ / ↓** 또는 Ctrl 키를 놓고 선택한 행을 드래그하면 함께 순서를 바꿉니다. **Space**는 선택 항목 ON/OFF, **Delete**는 삭제입니다.
- 삭제는 등록 복사본을 지우며 원래 가져온 폴더·ZIP은 지우지 않습니다. 다른 등록 항목과 공유한 복사본은 마지막 항목을 삭제할 때 지웁니다. 실행 중 적용 자료와 복구용 백업은 별도로 유지됩니다.

ZIP의 UTF-8 이름과 검증된 Info-ZIP Unicode Path 정보를 사용하며, 이름 해석은 화면 언어와 무관합니다. 일부 오래된 한국어 ZIP의 CP949 이름도 보완합니다. 인코딩 정보가 없는 오래된 ZIP은 이름을 확정할 수 없는 경우가 있으므로, 올바른 이름으로 압축을 풀고 폴더로 추가할 수 있습니다. 원래 ZIP이 남아 있으면 이전 버전에서 깨진 표시 이름도 복구합니다.

## 폴더·버튼·실행 중 교체

설치 폴더는 실행 중인 FM → 저장한 위치 → Steam 라이브러리 순으로 찾습니다. **게임 설치 폴더** 행에서 직접 선택할 수 있습니다. 파일 대상은 `fm_Data/StreamingAssets/aa/StandaloneWindows64`입니다.

**페이스·로고** 폴더는 Windows 문서의 `Sports Interactive/Football Manager 26/graphics`와 이전 설정을 사용합니다.

- **새로고침:** 페이스·로고와 `config.xml`을 다시 읽고 현재 게임 화면을 갱신합니다.
- **스킨 적용:** 선택한 스킨을 현재 실행 중인 게임 화면에 적용합니다.
- **전체 적용:** 페이스·로고를 먼저 갱신한 뒤 스킨을 적용합니다. 중간에도 진행 잠금을 유지합니다.

게임이 꺼져 있으면 하단 화면 적용 버튼은 잠기지만 스킨 ON/OFF는 사용할 수 있습니다. 게임 진행·저장·로드·경기 중에는 화면 적용을 잠그며, 스킨 선택 변경은 허용합니다.

“기존 자원이 없다”는 오류는 파일 이름 목록 검사와 다릅니다. 번들 내부의 개별 자원이 기존 스킨과 달라 실행 중 교체 연결을 만들지 못한다는 의미입니다. `26to24`에서 확인한 `FMCGridCompositionValidatorGameMode`, `GridCompositionValidatorGameMode` 누락 검사는 유지합니다. 실행 중 교체가 실패하면 FM을 종료하고 대기 중 설치 파일 저장이 끝난 뒤 다시 실행하도록 안내합니다.

현재 실행 중인 FM이 읽은 초기 스킨과 설치 파일 복원 기준을 구분합니다. 스킨 A로 FM을 시작한 뒤 A를 OFF 한 경우에도 화면은 A에서 기준 스킨으로 바꿔야 하므로, 단순히 “게임 시작 상태”로 되돌리는 것과 구분하여 처리합니다.

실행 중 갱신은 지원 모듈 해시가 일치하는 FM26 26.3.2용입니다. 변경 번들 100개 이하와 해당 실행의 자원 보관 한도가 적용됩니다. 기존 v10의 게임 상태 확인·게임 창 입력·계속 진행·자동 진행 타이머 잠금을 유지합니다. 다른 서드 파티와의 함수 변경 충돌이나 온라인 호스트의 모든 진행 경로를 검증한 업데이트는 아닙니다.

## 백업과 데이터

사용자 자료는 `%LOCALAPPDATA%/FM Graphics Reload`에 보관합니다.

- `skins.json`: 선택·순위·언어·종료 후 저장 대기 여부.
- `skins`: 등록 복사본.
- `installations/<설치위치 식별값>/backups`: 설치 파일 복구용 자료.
- `installations/<설치위치 식별값>/state.json`: 교체·추가한 파일과 복원 기준.
- `cache`, `logs`: 화면 적용 준비 자료와 작업 기록.

“원본”은 처음 교체하기 전에 설치 폴더에 있던 파일이며, 이미 수동으로 설치한 스킨일 수도 있습니다. 별도 원본 스킨 전체를 배포하지 않습니다. 적용 중인 파일을 되돌리려면 복구용 백업과 기록이 필요합니다.

변경 전 백업과 새 파일을 검증하고 작업 기록을 남깁니다. 실패하면 이미 바뀐 파일을 되돌리며, 강제 종료로 작업 기록이 남으면 다음 파일 저장 때 복구를 시도합니다. 파일 추가와 삭제도 복구 대상에 포함합니다. 복구가 막힌 경우 완료했다고 표시하지 않습니다.

## English quick guide

Use **Support development at the bottom right** to scan the supplied Ko-fi QR or open its donation page. All features are free; donations are optional. This panel never opens automatically. Payment is handled by the user on Ko-fi. English is the default for new settings. Existing language preferences are preserved. The native bridge is unchanged from 0.17–0.22.

Launch the single executable **FM Graphics Reload 1.0.1.exe**. Choose **English** at the top right. Version 1.0.0 includes an updated bridge; restart FM once if a bridge from 0.16 or earlier is already loaded. Validated live bundles are shared as dependency-connected groups, including compatible recovery bundles. Old inventory snapshots are released after replacement. Actual assets and Addressables operations remain retained because hidden references have not been proven absent. The fixed 40,000-link table is replaced by dynamic storage and binary lookup, with a 500,000-link safety bound. Prepared disk files are reused across game launches; each new process still loads and validates its own assets. Clearing the app cache removes these reusable files. Native icons follow monitor DPI, with 18 ICO sizes and PNGs up to 1024 pixels.

- ON/OFF, ordering and removal do not require a connected or idle game.
- With FM closed, those changes update installed files immediately, with backups and priority fallback.
- With FM running, selection changes are saved without replacing files used by FM. **Apply skins** refreshes the live game using separate copies.
- Keep the app open after FM exits to save queued changes automatically. Wait for the completion message before restarting FM. Closing this app first preserves the pending selection, but saving requires reopening the app with FM closed.
- Missing bundle filenames are checked before enabling. If FM is running, the app asks you to close FM and switch the skin ON again. Offline installation can add missing bundles; switching their last provider OFF removes the files the app added.
- Adding bundle files does not create a game resource catalog or certify a skin’s compatibility.
- Use the **▾** immediately to the right of **Delete** for **Reset skin baseline** or **Clear cache**. Both require FM to be closed.
- **Reset skin baseline** keeps manually edited installed files, turns all selections off and adopts the current installation as the new baseline. Old backups/history and the library remain available.
- **Clear cache** removes this app’s temporary skin/session data. Installed files, imported skins, recovery backups and ordinary error logs are preserved.
- **Retry pending file save** appears in the dropdown when a selection is waiting to be written.
- The lower refresh/apply buttons require a connected idle FM. If live switching fails, close FM, wait for the pending file save, then relaunch.
- Settings and backups are in `%LOCALAPPDATA%/FM Graphics Reload`. Imported skin names are decoded independently of the UI language.
- Version 1.0.0 checks loaded script-backed assets, retries an incomplete first load once using fresh copies, and initializes grid settings before switching. It rebuilds menu layers and open panels using FM's template resolver, preserving their data bindings. This supports layout changes such as 26to24's left navigation. Unresolved child templates stop the replacement before it reaches the screen.

## 개발

설치 파일 변경: `installed_skins.py`. 실행 중 선택·화면 반영: `skin_library.py`, `full_skins.py`. 메모리 재사용: `skin_pool.py`. 준비 파일 캐시: `prepared_skin_cache.py`. 스크립트 자원 검증: `bundle_inventory.py`. 기준/캐시 초기화: `skin_maintenance.py`. UI: `interface.py`, `i18n.py`, `window_icon.py`. 1.0.0은 조회 자료 수명 관리와 검색을 개선한 새 연결 DLL을 포함합니다.

빌드 방법은 README.md를 참고하세요. 단일 EXE에 아이콘·연결 모듈·지원 빌드 정보·라이선스를 포함합니다. 삭제 오른쪽 ▾에서 오픈소스 라이선스를 확인할 수 있습니다.

## 단일 실행파일

배포에는 EXE 하나만 필요합니다. 실행 시 내장 자원을 임시 폴더로 풀고, 게임이 사용하는 연결 모듈은 LocalAppData의 FM Graphics Reload/runtime/<해시>에 보관합니다. 설정·스킨·백업도 기존 LocalAppData 위치를 계속 사용합니다. 소스는 MIT 라이선스로 제공합니다.
